# Examples

Runnable examples live in **`ksef-demo/`** (the live-validation harness).

For minimal usage snippets, see the [Quick start section in the root README](../README.md#quick-start).

The `examples/` folder is reserved for future standalone snippets (Spring Boot integration, async patterns, custom retry strategies). Track progress in PLAN 9.1.
