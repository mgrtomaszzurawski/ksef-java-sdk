<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.github.mgrtomaszzurawski</groupId>
    <artifactId>ksef-java-sdk</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>

    <name>KSeF Java SDK</name>
    <description>Java SDK for the Polish National e-Invoicing System (KSeF) REST API v2</description>
    <url>https://github.com/mgrtomaszzurawski/ksef-java-sdk</url>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <developers>
        <developer>
            <id>mgrtomaszzurawski</id>
            <name>Tomasz Zurawski</name>
        </developer>
    </developers>

    <scm>
        <connection>scm:git:https://github.com/mgrtomaszzurawski/ksef-java-sdk.git</connection>
        <developerConnection>scm:git:ssh://github.com/mgrtomaszzurawski/ksef-java-sdk.git</developerConnection>
        <url>https://github.com/mgrtomaszzurawski/ksef-java-sdk</url>
    </scm>

    <modules>
        <module>ksef-client</module>
        <module>ksef-demo</module>
        <module>ksef-examples</module>
    </modules>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.build.outputTimestamp>2026-04-03T00:00:00Z</project.build.outputTimestamp>
        <gpg.plugin.version>3.2.7</gpg.plugin.version>
        <central.publishing.plugin.version>0.7.0</central.publishing.plugin.version>
    </properties>

    <!--
        Release profile — activated via `mvn ... -Prelease` for Maven Central
        publishing. Signs all artifacts with GPG and uploads via Sonatype's
        central-publishing-maven-plugin (the OSSRH replacement, Central Portal).

        Requires GPG key configured locally and `<server>` credentials in
        ~/.m2/settings.xml with id `central` containing
        Sonatype Central Portal user token.
    -->
    <profiles>
        <profile>
            <id>release</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-gpg-plugin</artifactId>
                        <version>${gpg.plugin.version}</version>
                        <executions>
                            <execution>
                                <id>sign-artifacts</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>sign</goal>
                                </goals>
                                <configuration>
                                    <gpgArguments>
                                        <arg>--pinentry-mode</arg>
                                        <arg>loopback</arg>
                                    </gpgArguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                    <plugin>
                        <groupId>org.sonatype.central</groupId>
                        <artifactId>central-publishing-maven-plugin</artifactId>
                        <version>${central.publishing.plugin.version}</version>
                        <extensions>true</extensions>
                        <configuration>
                            <publishingServerId>central</publishingServerId>
                            <autoPublish>false</autoPublish>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>com.mycila</groupId>
                    <artifactId>license-maven-plugin</artifactId>
                    <version>4.6</version>
                    <configuration>
                        <licenseSets>
                            <licenseSet>
                                <header>LICENSE-HEADER.txt</header>
                                <includes>
                                    <include>src/main/java/**/*.java</include>
                                    <include>src/test/java/**/*.java</include>
                                </includes>
                            </licenseSet>
                        </licenseSets>
                    </configuration>
                    <executions>
                        <execution>
                            <id>license-headers</id>
                            <phase>process-sources</phase>
                            <goals>
                                <goal>format</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>

</project>
